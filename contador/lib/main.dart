// Instalaciones paquetes propios y de terceros
import 'package:flutter/material.dart';


// Nosotros escribimos, (ctrl + . en el error para importar auto)
 // import 'src/app.dart';
import 'src/app.dart';

void main() {
  runApp(new MyApp());
}